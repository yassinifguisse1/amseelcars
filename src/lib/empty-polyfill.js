/**
 * Empty stub replacing Next.js `polyfill-module`.
 * Lighthouse flags ~11 KiB of unused Array/Object/String polyfills that Next
 * ships unconditionally; our browserslist already assumes modern engines.
 * Keep this file empty — do not re-add core-js style polyfills here.
 */
